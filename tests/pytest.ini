[pytest]
usefixtures = get_test_name
log_cli = True
log_cli_format = %(asctime)s %(levelname)s %(message)s

# define number of workers for pytest-xdist
# create html report
addopts = -n1 --html=report.html --self-contained-html

env =
    EMAIL_PSSWD =
    EMAIL_USR =
    EMAIL_RECIPIENT =